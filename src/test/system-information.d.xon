

~ battery {
    #{
        si = require("systeminformation")
        #{const battery = await si.battery();}
    }
}

~ battery:
    #si = require("systeminformation")
    #const battery = await si.battery();